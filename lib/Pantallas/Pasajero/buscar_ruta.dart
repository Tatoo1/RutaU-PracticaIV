import 'package:flutter/material.dart';

class BuscarRuta extends StatelessWidget {
  const BuscarRuta({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Buscar Ruta')),
    );
  }
}
