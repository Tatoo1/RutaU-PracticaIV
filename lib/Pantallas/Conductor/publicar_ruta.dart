import 'package:flutter/material.dart';

class PublicarRuta extends StatelessWidget {
  const PublicarRuta({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Publicar Ruta')),
    );
  }
}
