import 'package:flutter/material.dart';

class GestionarVehiculos extends StatelessWidget {
  const GestionarVehiculos({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Gestionar Vehículos')),
    );
  }
}
